<resources>
    <!-- windowBackground follows light/dark via -night color qualifier: any
         frame exposed between splash dismissal and Compose's first draw stays
         seamless with both the splash and the incoming UI. -->
    <style name="Theme.PocketDiary" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
    </style>

    <!-- System splash: background + logo auto-switch via resource qualifiers
        (drawable-night/splash_logo = white version, splash_background = black).
        Dismisses on first frame; kept >=400ms via keepOnScreenCondition. -->
    <style name="Theme.PocketDiary.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.PocketDiary</item>
    </style>
</resources>
