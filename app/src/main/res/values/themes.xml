<resources>
    <!-- windowBackground pinned to black: the splash is black, so any frame
         exposed between splash dismissal and Compose's first draw stays
         seamless. Without this, the Light parent theme's near-white
         windowBackground flashes at the top band in dark mode. -->
    <style name="Theme.PocketDiary" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>

    <!-- System splash: black background + centered logo, dismisses on first frame.
         Covers the cold-start flash; kept >=400ms via keepOnScreenCondition. -->
    <style name="Theme.PocketDiary.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/black</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_logo</item>
        <item name="postSplashScreenTheme">@style/Theme.PocketDiary</item>
    </style>
</resources>
